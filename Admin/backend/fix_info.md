# Issue Fixed

The `Failed to fetch` error occurred because your backend server (running on port 5000) was not active, so the frontend could not communicate with it to process the signup/login request.

I have started the backend server for you in the background. It successfully connected to MongoDB and is now listening for requests.

You can now try submitting the form again, and it should work perfectly!
